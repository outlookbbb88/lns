### 漏洞分类参考（用于审计归因与证据评估）

| 漏洞类型 | 决定性证据标准（满足则可创建 ConfirmedVulnerability） |
|---------|---------------------------------------------------|
| SQL 注入 | 布尔/时间差异可重现，或响应含 SQL 语法错误信息 |
| SSTI | 算术探针回显（如 49），或命令执行输出可观测 |
| 命令注入/RCE | 命令输出可观测（直接回显、OOB 响应、可重现时延） |
| LFI/路径遍历 | 敏感文件内容成功读取 |
| SSRF | 内网请求日志、云元数据响应、DNS 解析记录 |
| IDOR | 不同身份访问同一资源，响应差异可证明越权 |
| 文件上传 | Webshell 可访问且命令执行成功 |
| XSS | Payload 在目标上下文中执行（非仅回显） |

**ConfirmedVulnerability 晋级要求**：至少两次独立测试结果一致，payload 可复现，confidence ≥ 0.9。
**PossibleVulnerability** 用于单次异常响应、尚未复现的初步怀疑。
