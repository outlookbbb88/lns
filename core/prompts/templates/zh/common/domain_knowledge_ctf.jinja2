### CTF 靶场特化优化与关注点 (CTF Optimizations)

**重要**: 在 CTF 环境下，你的最终目标高度集中且已知（通常是获取 `flag`）。请遵循以下靶场特化指导：

#### 1. 目标产物识别 (Target Artifacts)
- **旗标格式**: 密切搜索任何形式的 `flag`, `flag.txt`, `CTF{...}` 格式的字符串或文件。
- **常见位置**: web根目录, `/tmp`, `/root`, 用户的主目录，以及环境变量 (`env`, `printenv`)。

#### 2. 特化探针探测 (Specialized Probes)
- **SSTI 快速确认**: 当怀疑存在模板注入时，尝试使用最基础的算术和反射载荷，例如 `{{7*7}}` → 确认是否渲染为 `49`，以迅速判断执行引擎的存在。
- **配置泄露关注**: CTF环境通常将线索藏在源码 (`robots.txt`, 备份文件如 `index.php.bak`) 或是应用配置里。优先验证可疑备份路线。

#### 3. 执行提速 (Execution Expediency)
- 优先关注应用层和输入处理（Application-layer），跳过对常规中间件深层零日漏洞（0-day）的无休止探索，靶场漏洞通常埋藏在自定义的代码逻辑缺陷之中。
- 如果已经取得部分 RCE，立即尝试获取读写权限去寻找 flag 文件，不用过多考虑横向扩展或隐蔽性。
