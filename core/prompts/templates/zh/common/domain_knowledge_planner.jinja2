### 战术知识库（规划视角）

#### 通用利用原则
1. **漏洞组合思维**: 优先思考多漏洞组合链，例如：LFI + 日志投毒 → RCE；IDOR + XSS → 账户接管；信息泄露 + 任意文件读取 → 源码审计 → RCE。
2. **业务功能 → 漏洞方向参考**（非穷举，需结合具体实现判断）：
   - 认证/登录 → SQLi、时序枚举、JWT 算法混淆、OAuth 流程缺陷
   - 文件上传 → Webshell、文件名 LFI、MIME 绕过、竞争条件
   - 查询/搜索 → SQLi、NoSQLi、SSTI
   - 重定向/代理 → SSRF、URL 解析混淆
   - 权限控制 → IDOR、水平/垂直越权、功能级别访问缺陷
3. **漏洞利用路径优先级**: RCE > 数据提取 > 权限提升 > 敏感信息泄露。
4. **收敛策略**: 一旦确认高价值漏洞，立即收敛资源进行深入利用，暂停无关探索。

#### 目标产物位置（规划收敛参考）
- 环境变量、应用配置文件、数据库凭据、Web 根目录敏感文件。
- 具体路径由 Executor 根据目标上下文动态探测，**规划层不做路径硬编码假设**。

{% if use_ctf_optimizations %}
#### CTF 场景规划要点
- 目标高度集中（通常为 flag 文件），优先理解业务逻辑而非基础设施扫描。
- 漏洞通常在：认证逻辑、权限控制、业务流程、数据验证、自定义代码缺陷。
{% endif %}
